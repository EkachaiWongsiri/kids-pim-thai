# 📋 บันทึกรายการ Bug & ปัญหาที่ต้องแก้ไข (Bug Backlog)

รายการปัญหาและข้อเสนอแนะที่รวบรวมไว้เพื่อดำเนินการแก้ไขพร้อมกันในรอบถัดไป:

---

### 1. 🔤 การแจ้งเตือนเมื่อเปิดปุ่ม Caps Lock (Caps Lock Warning Overlay)
* **ปัญหา (Issue):** 
  - เมื่อเด็กเผลอกดปุ่ม `Caps Lock` บนคีย์บอร์ดจริงโดยไม่รู้ตัว อักขระที่พิมพ์จะกลายเป็นตัวพิมพ์ใหญ่ (EN) หรืออักขระแถวบน/Shift (TH) ทำให้ไม่ตรงกับตัวอักษรของคำที่กำลังตก
  - เด็กจะไม่สามารถพิมพ์ตัวอักษรได้ และไม่เข้าใจว่าเกิดจากอะไร
* **สิ่งที่ต้องทำ (Solution):**
  - ดักจับสถานะ Caps Lock ผ่าน `e.getModifierState('CapsLock')` ใน Event Listener (`keydown` / `keyup` / `window focus`)
  - แสดงแบนเนอร์ / การ์ดเตือนเด่นชัดบนหน้าจอ (เช่น *"⚠️ ปุ่ม Caps Lock กำลังเปิดอยู่! กรุณากดปุ่ม Caps Lock บนแป้นพิมพ์อีกครั้งเพื่อปิด"*)
  - ปิดการแจ้งเตือนทันทีเมื่อตรวจพบว่า Caps Lock ถูกปิดแล้ว

---

### 2. ⏸️ หยุดเกมชั่วคราวอัตโนมัติเมื่อเปิด Modal (Auto Pause on Modal Open)
* **ปัญหา (Issue):** 
  - เวลาผู้เล่นกดเปิด Modal ใดๆ ที่ขึ้นมาบังหน้าจอ (เช่น เลือกด่าน, คลังคำศัพท์, ตั้งค่าเกม, เกี่ยวกับเรา, ติดต่อเรา, นโยบายความเป็นส่วนตัว ฯลฯ) ลูกโป่งคำในเกมยังคงตกลงมาเรื่อยๆ ในพื้นหลัง
  - ทำให้ผู้เล่นเสียพลังชีวิต (💔) โดยไม่ตั้งใจขณะที่กำลังดูหน้าต่างอื่น
* **สิ่งที่ต้องทำ (Solution):**
  - ตั้งเงื่อนไขให้เกมเข้าสู่สถานะ `isPaused = true` โดยอัตโนมัติทันทีที่มี Modal ใดๆ เปิดอยู่ (`isStageSelectOpen`, `isCustomWordsOpen`, `isSettingsOpen`, หรือ `activePortalModal !== null`)
  - คืนสถานะเกมหรือให้ผู้เล่นกด Resume เล่นต่อเมื่อปิด Modal ทั้งหมดเรียบร้อยแล้ว

---

*(บันทึกไว้ ณ วันที่ 12 กันยายน 2026 - รอการสั่งแก้ไขแบบรวมชุด)*
