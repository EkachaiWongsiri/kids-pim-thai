# 📋 บันทึกรายการ Bug & แผนพัฒนาฟีเจอร์ใหม่ (Backlog & Roadmap)

เอกสารรวบรวมรายการปัญหา (Bugs) และฟังก์ชันใหม่ที่ต้องการพัฒนา (Feature Requests) เพื่อเตรียมดำเนินการแก้ไขและอัปเดตรวดเดียวในรอบถัดไป:

---

## 🐛 ส่วนที่ 1: รายการ Bug ที่ต้องแก้ไข (Bug Fixes)

### 1. 🔤 การแจ้งเตือนเมื่อเปิดปุ่ม Caps Lock (Caps Lock Warning Overlay)
* **ปัญหา (Issue):** 
  - เมื่อเด็กเผลอกดปุ่ม `Caps Lock` บนคีย์บอร์ดจริงโดยไม่รู้ตัว อักขระที่พิมพ์จะกลายเป็นตัวพิมพ์ใหญ่ (EN) หรืออักขระแถวบน/Shift (TH) ทำให้ไม่ตรงกับตัวอักษรของคำที่กำลังตก
  - เด็กจะไม่สามารถพิมพ์ตัวอักษรได้ และไม่เข้าใจว่าเกิดจากอะไร
* **แนวทางแก้ไข (Solution):**
  - ดักจับสถานะ Caps Lock ผ่าน `e.getModifierState('CapsLock')` ใน Event Listener (`keydown` / `keyup` / `window focus`)
  - แสดงแบนเนอร์ / Overlay การ์ดเตือนเด่นชัดบนหน้าจอ (เช่น *"⚠️ ปุ่ม Caps Lock กำลังเปิดอยู่! กรุณากดปุ่ม Caps Lock บนแป้นพิมพ์อีกครั้งเพื่อปิด"*)
  - ซ่อนการแจ้งเตือนทันทีเมื่อตรวจพบว่า Caps Lock ถูกปิดแล้ว

---

### 2. ⏸️ หยุดเกมชั่วคราวอัตโนมัติเมื่อเปิด Modal (Auto Pause on Modal Open)
* **ปัญหา (Issue):** 
  - เวลาผู้เล่นกดเปิด Modal ใดๆ ที่ขึ้นมาบังหน้าจอ (เช่น เลือกด่าน, คลังคำศัพท์, ตั้งค่าเกม, เกี่ยวกับเรา, ติดต่อเรา, นโยบายความเป็นส่วนตัว ฯลฯ) ลูกโป่งคำในเกมยังคงตกลงมาเรื่อยๆ ในพื้นหลัง
  - ทำให้ผู้เล่นเสียพลังชีวิต (💔) โดยไม่ตั้งใจขณะที่กำลังดูหน้าต่างอื่น
* **แนวทางแก้ไข (Solution):**
  - ตั้งเงื่อนไขให้เกมเข้าสู่สถานะ `isPaused = true` โดยอัตโนมัติทันทีที่มี Modal ใดๆ เปิดอยู่ (`isStageSelectOpen`, `isCustomWordsOpen`, `isSettingsOpen`, หรือ `activePortalModal !== null`)
  - คืนสถานะเกมหรือให้ผู้เล่นกด Resume เล่นต่อเมื่อปิด Modal ทั้งหมดเรียบร้อยแล้ว

---

## 🚀 ส่วนที่ 2: ฟังก์ชันและฟีเจอร์ใหม่ (New Features)

### 3. 📦 ระบบส่งออกและนำเข้าข้อมูลไฟล์ `kids-pim-thai.json` (Export & Import Data)
* **เป้าหมาย (Objective):** 
  - อำนวยความสะดวกให้คุณครูในโรงเรียนหรือผู้ปกครอง สามารถเตรียมคำศัพท์และการตั้งค่าจากเครื่องหนึ่ง แล้ว Export ออกมาเป็นไฟล์ `kids-pim-thai.json` เพื่อนำไป Import ลงเครื่องคอมพิวเตอร์ทุกเครื่องในห้องเรียน/แท็บเล็ตของเด็กๆ ได้อย่างสะดวก
* **รายละเอียดการทำงาน (Specifications):**
  * **3.1 เลือกข้อมูลที่จะ Export ได้อย่างอิสระ (Granular Export Selection):**
    - มีหน้าต่างให้เลือก Checkbox ก่อนดาวน์โหลดไฟล์ JSON:
      - [x] คลังคำศัพท์ที่กำหนดเอง (Custom Words & Word Sets)
      - [x] ด่านที่สร้างขึ้นเอง (Custom Stages)
      - [x] การตั้งค่าเกม (Game Settings: ความเร็ว, เป้าหมายคำต่อด่าน, จำนวนคำที่ตกลงมาพร้อมกัน, สถานะเสียง)
  * **3.2 การจัดการเมื่อข้อมูลซ้อนทับกันตอน Import (Conflict Resolution):**
    - เมื่อผู้ใช้กด Import ไฟล์ `kids-pim-thai.json` เข้ามา หากตรวจพบข้อมูลเดิมที่มีอยู่หรือ Sequence ID ซ้อนทับกัน ระบบจะมี Dialog ให้เลือกวิธีจัดการ:
      - 1) **แทนที่ทั้งหมด (Overwrite / Replace All):** ล้างข้อมูลเดิมและแทนที่ด้วยข้อมูลจากไฟล์ใหม่ทั้งหมด
      - 2) **รวมข้อมูลเข้าด้วยกัน (Merge & Append):** นำด่านและคำศัพท์ใหม่มาต่อท้ายของเดิม โดยระบบจะ Re-index ลำดับด่าน (Sequence ID) ใหม่อัตโนมัติ พร้อมแจ้งเตือนให้ผู้ใช้ทราบว่าลำดับด่านอาจมีการปรับเปลี่ยนจากต้นฉบับ
      - 3) **ข้ามรายการที่ซ้ำ (Skip Duplicates):** เพิ่มเฉพาะคำศัพท์และด่านที่ไม่เคยมีมาก่อนในเครื่อง

---

### 4. 📚 ระบบบันทึกและจัดการชุดคำศัพท์หลายหมวดหมู่ (Multi-Preset Word Bank)
* **เป้าหมาย (Objective):** 
  - ให้ผู้ใช้สามารถสร้างและบันทึกชุดคำศัพท์แยกตามหมวดหมู่ได้หลายชุด (นอกเหนือจากด่านพื้นฐานที่มี) เช่น "คำศัพท์บทที่ 1", "คำควบกล้ำ ป.2", "หมวดสัตว์น้ำ", "Daily English Words"
* **รายละเอียดการทำงาน (Specifications):**
  - มีหน้า UI สำหรับจัดการชุดคำศัพท์: สร้างชุดใหม่, ตั้งชื่อชุด, แก้ไขรายการคำ, ลบชุดคำศัพท์
  - สามารถสลับเลือกชุดคำศัพท์ที่ต้องการเล่นได้ตลอดเวลา โดยไม่ต้องพิมพ์คำใหม่ซ้ำๆ
  - ข้อมูลชุดคำศัพท์ทั้งหมดถูกบันทึกลง LocalStorage และเชื่อมโยงเข้ากับระบบ Export / Import ในข้อ 3 โดยอัตโนมัติ

---

*(บันทึกความต้องการล่าสุด ณ วันที่ 12 กันยายน 2026 - รอดำเนินการพัฒนาพร้อมชุดแก้ Bug)*
